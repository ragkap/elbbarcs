# Allow everything. The page is not intended for indexing, but in
# practice noindex/Disallow blocked link-preview scrapers (Facebook,
# Twitter) and the URL is non-discoverable anyway. Plain allow keeps
# previews working everywhere.

User-agent: *
Allow: /
